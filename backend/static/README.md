# static/

Reserved for project static assets: CSS (Tailwind output), JavaScript
(ES6 modules), GSAP/Lottie animation assets, Chart.js configurations, and
images. Collected into `static_collected/` via `collectstatic` for
production serving through WhiteNoise.

**Status:** Empty foundation placeholder. No static assets have been added
yet.

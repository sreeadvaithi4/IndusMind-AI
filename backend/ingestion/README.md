# ingestion/

Reserved for document ingestion pipelines: file upload handling, OCR
(Tesseract), PDF parsing (PyMuPDF), Word document parsing (python-docx),
and tabular data parsing (pandas).

**Status:** Empty foundation placeholder. No ingestion pipeline has been
implemented yet.

# api/

Reserved for Django REST Framework serializers, viewsets, and routers that
expose IndusMind AI functionality (document ingestion, RAG query, knowledge
graph queries, agent orchestration) as REST endpoints.

**Status:** Empty foundation placeholder. No API endpoints have been built
yet.

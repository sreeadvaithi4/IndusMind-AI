# templates/

Reserved for Django HTML templates (landing page, chatbot UI, dashboards),
rendered using Django's template engine and styled with Tailwind CSS.

**Status:** Empty foundation placeholder. No templates have been built yet.
